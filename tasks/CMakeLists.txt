# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(transformerTaskLib)
ADD_LIBRARY(${TRANSFORMER_TASKLIB_NAME} SHARED 
    ${TRANSFORMER_TASKLIB_SOURCES})

add_dependencies(${TRANSFORMER_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${TRANSFORMER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${TRANSFORMER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${TRANSFORMER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${TRANSFORMER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${TRANSFORMER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${TRANSFORMER_TASKLIB_HEADERS}
    DESTINATION include/orocos/transformer)

